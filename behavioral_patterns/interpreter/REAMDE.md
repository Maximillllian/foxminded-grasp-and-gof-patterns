Написать аналог SQL языка, который читает команды из командной строки.

Должны быть реализованы команды SELECT, FROM, WHERE

Например, можно написать запрос
SELECT "name" FROM "some_table" WHERE "age" = "13"